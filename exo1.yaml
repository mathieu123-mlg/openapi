openapi: 3.1.0
info:
  title: Exercice 1
  description: Décrire une API simple de "Ping"
  version: 1.0.0
servers:
  - url: 'http://localhost:8000'
paths:
  /ping:
    get:
      responses:
        '200':
          description: OK
          content:
            text/plain:
              schema:
                type: string
                example: 'pong'
