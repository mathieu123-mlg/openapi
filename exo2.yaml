openapi: 3.1.0
info:
  title: Exercice 2
  description: Manipuler les paramètres et les réponses
  version: 1.0.0
servers:
  - url: 'http://localhost:8000'
paths:
  /users:
    get:
      description: Retourne une liste paginée des utilisateurs
      parameters:
        - name: page
          in: query
          description: Numéro de la page à récupérer
          required: false
          schema:
            type: integer
            minimum: 1
            default: 1
            example: 1
        - name: size
          in: query
          description: Nombre d'utilisateurs par page
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 20
            example: 20
      responses:
        '200':
          description: Liste des utilisateurs retrouvés
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/User'
              example:
                - id: 1
                  name: "Alice"
                  email: "alice@gmail.com"
                - id: 2
                  name: "Bob"
                  email: "bob@gmail.com"
                - id: 3
                  name: "Charlie"
                  email: "charlie@gmail.com"
                - id: 4
                  name: "Diana"
                  email: "diana@gmail.com"
                - id: 5
                  name: "Eve"
                  email: "eve@gmail.com"
        '400':
          description: Mauvais types de paramètres fournis
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: "Bad types for provided query parameters"

components:
  schemas:
    User:
      type: object
      required:
        - id
        - name
        - email
      properties:
        id:
          type: integer
          description: Identifiant unique de l'utilisateur
          example: 1
        name:
          type: string
          description: Nom complet de l'utilisateur
          example: "Alice"
        email:
          type: string
          format: email
          description: Adresse email de l'utilisateur
          example: "alice@gmail.com"

    Error:
      type: object
      required:
        - error
      properties:
        error:
          type: string
          description: Message d'erreur décrivant le problème
          example: "Bad types for provided query parameters"